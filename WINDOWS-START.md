# Windows 一键启动

普通用户只需要双击根目录下的 `Start-Gate-Scanner.cmd`。

启动逻辑：

1. 如果缺少构建文件，脚本会自动补一次构建。
2. 脚本会在后台启动 API 和前端预览服务。
3. 然后用 Edge 或 Chrome 的独立应用窗口打开系统界面。
4. 用户关闭这个窗口后，后台的前端和 API 也会一起退出。

补充说明：

- 如果你改了代码，建议先执行一次 `pnpm desktop:prepare`，再双击启动。
- 如果提示端口 `3001` 或 `4173` 被占用，先关闭旧的 Node 进程，再重新双击。
